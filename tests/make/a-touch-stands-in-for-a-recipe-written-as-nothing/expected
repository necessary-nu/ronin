oracle GNU Make 4.4.1
outcome success
file Makefile kept 7
| # The same rule where the recipe is empty as written rather than after
| # expansion: `b: c;` declares a recipe of no lines, and a declared recipe is
| # what `-t` stands in for.
| .SUFFIXES:
| a: b
| 	@echo made > $@
| b: c;
file a touched 0
file b touched 0
file c kept 1
| seed
