oracle GNU Make 4.4.1
outcome success
file Makefile kept 6
| # `.ONESHELL` changes how many shells the recipe gets and nothing about what
| # each line holds.
| .ONESHELL:
| all:
| 	@printf '%s' 'a\
| 	b' | od -An -c > out
file out touched 1
|    a   \  \n   b
