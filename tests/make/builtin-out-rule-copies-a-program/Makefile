# `%.out: %` is one of the four rules `default.c` writes as a pattern rather
# than as a suffix, and it needs no tool: BSD Make spells a program `foo.out`
# and has no null-suffix rules to make one.
all: foo.out
