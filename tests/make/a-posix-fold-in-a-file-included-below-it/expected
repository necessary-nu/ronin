oracle GNU Make 4.4.1
outcome success
file Makefile kept 5
| # The flag belongs to the read rather than to the file: a line an `include`
| # brings in after `.POSIX:` was read is folded the POSIX way.
| .POSIX:
| include inc.mk
| all: ; @printf '%s' '$(A)' | od -An -c > out
file inc.mk kept 2
| A = a   \
|   b
file out touched 1
|    a                   b
