# A command-line assignment outranks the makefile, and that includes the
# assignment the makefile never wrote: recording the first target does not
# overwrite it.
first: ; @printf '%s\n' 'first ran' > first.out
second: ; @printf '%s\n' 'second ran' > out
