oracle GNU Make 4.4.1
outcome failure
file Makefile kept 6
| # The switch arrives from the makefile rather than the command line, and the
| # rules it withholds are installed after the whole read — so it withholds them
| # just as the command line would.
| MAKEFLAGS += -r
| 
| all: xxx.o
file xxx.c kept 1
| int main(void) { return 0; }
