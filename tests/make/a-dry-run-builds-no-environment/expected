oracle GNU Make 4.4.1
outcome success
file Makefile kept 4
| # A dry run prints the recipe and returns before target_environment is
| # reached, so the exported command-line value is never read and `out` is never
| # written.
| all:; @echo ran > out
