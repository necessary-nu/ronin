oracle GNU Make 4.4.1
outcome success
file Makefile kept 5
| # And the same goal found current, which is the run that has nothing to do at
| # all: nothing is made and nothing is written here.
| VPATH = build
| 
| out.o: out.c ; @printf '%s from %s\n' '$@' '$<' > $@
file build/out.o kept 1
| old
file out.c kept 1
| source
