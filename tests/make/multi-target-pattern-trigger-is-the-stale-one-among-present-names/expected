oracle GNU Make 4.4.1
outcome success
file Makefile kept 4
| # Both names are there and only a.h is behind a.in, so a.h is what sends
| # the recipe to the shell and what $@ names.
| all: a.c a.h
| %.c %.h: %.in ; echo 'at=$@ stem=$*' >> log ; cat $< > $*.c ; cat $< > $*.h
file a.c touched 1
| body
file a.h touched 1
| body
file a.in kept 1
| body
file log touched 1
| at=a.h stem=a
