oracle GNU Make 4.4.1
status 0
file Makefile kept 5
| # A backslash before a colon keeps it inside the target's name rather than
| # ending the target list.
| all: pre\:foo
| 
| pre\:foo: ; @printf '%s\n' '$@' > out
file out touched 1
| pre:foo
