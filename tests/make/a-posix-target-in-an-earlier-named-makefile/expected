oracle GNU Make 4.4.1
outcome success
file Makefile kept 5
| # The same across the makefiles `-f` names: `.POSIX:` in the first is read
| # before the second is, so the second folds the POSIX way.
| A = a   \
|   b
| all: ; @printf '%s' '$(A)' | od -An -c > out
file one.mk kept 1
| .POSIX:
file out touched 1
|    a                   b
