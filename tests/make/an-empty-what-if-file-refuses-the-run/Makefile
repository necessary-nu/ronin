# The same gate on a switch this Make accepts and does nothing with. `-W` names a
# file to pretend was just written; the value goes nowhere here, and the refusal
# of an empty one still does — a build GNU Make will not run must not run.
all: ; @printf 'built\n' > out
