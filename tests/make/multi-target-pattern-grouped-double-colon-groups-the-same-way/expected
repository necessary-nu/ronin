oracle GNU Make 4.4.1
outcome success
file Makefile kept 5
| # `&::' on a pattern rule reaches the same grouping, and one record of it runs
| # once for both names.
| all: hello.tab.c hello.tab.h
| hello.y: ; echo body > $@
| %.tab.c %.tab.h &:: %.y ; echo 'at=$@' >> log ; cat $< > $*.tab.c ; cat $< > $*.tab.h
file hello.tab.c touched 1
| body
file hello.tab.h touched 1
| body
file hello.y touched 1
| body
file log touched 1
| at=hello.tab.c
