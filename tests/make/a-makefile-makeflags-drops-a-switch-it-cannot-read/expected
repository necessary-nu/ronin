oracle GNU Make 4.4.1
outcome success
file Makefile kept 7
| MAKEFLAGS += -W
| MAKEFLAGS += --jobserver-style=
| MAKEFLAGS += -jabc
| MAKEFLAGS += --shuffle=
| MAKEFLAGS += --include-dir=
| MAKEFLAGS += -zz --frobnicate
| all: ; @echo built > out
file out touched 1
| built
