oracle GNU Make 4.4.1
outcome success
file Makefile kept 3
| # A tab between two words is a separator like a space is.
| list := a b	c d
| all: ; @printf '%s\n' '$(sort $(list))' '$(words $(sort $(list)))' > out
file out touched 2
| a b c d
| 4
