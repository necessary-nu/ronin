oracle GNU Make 4.4.1
outcome success
file Makefile kept 6
| # A goal reaches the search without a prerequisite list to be rewritten in, and
| # it is answered the same way: the copy the search found is out of date, so the
| # goal is made here and the copy is left where it was.
| VPATH = build
| 
| out.o: out.c ; @printf '%s from %s\n' '$@' '$<' > $@
file build/out.o kept 1
| old
file out.c kept 1
| source
file out.o touched 1
| out.o from out.c
