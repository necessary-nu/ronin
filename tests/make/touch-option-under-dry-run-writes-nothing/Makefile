# `-n` outranks `-t`: a run told to change nothing changes nothing, so the
# target is neither made nor dated and the file that is not there stays absent.
all: out.txt
out.txt: in.txt ; @echo REAL > out.txt
