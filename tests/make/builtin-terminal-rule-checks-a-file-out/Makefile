# The catalogue's terminal rules are the one place Make makes a file with no
# source to build from: `%:: %,v` checks it out of RCS. `CO` is given here so
# the case records the checkout rather than needing one installed.
all: foo
