# Under `.ONESHELL:` the recipe is one script and the prefixes are read off its
# first line, so a `+` there marks the whole of it. The question has no line
# left to answer on, and every line runs.
.ONESHELL:
all:
	+@echo plus > plus.txt
	@echo ordinary > ordinary.txt
