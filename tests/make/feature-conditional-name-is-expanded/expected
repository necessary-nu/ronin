oracle GNU Make 4.4.1
status 0
file Makefile kept 18
| # The argument of ifdef is expanded before it is looked up, so the name being
| # asked about can itself be computed.
| foo = set
| FOO = foo
| F = f
| 
| first = no
| second = no
| 
| ifdef $(FOO)
| first = yes
| endif
| 
| ifdef $(F)oo
| second = yes
| endif
| 
| all: ; @printf '%s %s\n' '$(first)' '$(second)' > out
file out touched 1
| yes yes
