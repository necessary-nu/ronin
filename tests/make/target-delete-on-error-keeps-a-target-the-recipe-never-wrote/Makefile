# Deletion is for what the recipe half-made. This one failed before writing
# anything, so the file that was already there is the one that was already
# there, and .DELETE_ON_ERROR has nothing to take back.
.DELETE_ON_ERROR:
all: out
out: dep ; exit 1
