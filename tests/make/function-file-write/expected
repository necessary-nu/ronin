oracle GNU Make 4.4.1
status 0
file Makefile kept 8
| define A
| a
| b
| endef
| B = c d
| $(file >file.out,$(A))
| $(foreach L,$(B),$(file >>     file.out,$(L)))
| all: ; @printf '%s\n' 'done' > out
file file.out touched 4
| a
| b
| c
| d
file out touched 1
| done
