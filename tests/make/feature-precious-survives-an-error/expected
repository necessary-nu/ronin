oracle GNU Make 4.4.1
outcome failure
file Makefile kept 8
| # .PRECIOUS names what to keep even so, and it matches a pattern as readily as
| # a name.
| .DELETE_ON_ERROR:
| .PRECIOUS: %.bar
| 
| all: foo.bar
| 
| %.bar: ; @printf '%s\n' partial > $@; exit 1
file foo.bar touched 1
| partial
