oracle GNU Make 4.4.1
status 0
file Makefile kept 5
| # A backslash before a space keeps a target's name in one piece rather than
| # splitting it into two names.
| all: two\ words
| 
| two\ words: ; @printf '%s\n' '$@' > out
file out touched 1
| two words
