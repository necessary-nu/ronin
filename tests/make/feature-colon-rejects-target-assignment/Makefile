# A second colon after the rule separator is static-pattern syntax; it cannot
# occur in a target-specific variable name unless it belongs to :=, ::= or
# :::=.  This line therefore fails as a malformed static-pattern rule.
all:FOO:BAR=good
