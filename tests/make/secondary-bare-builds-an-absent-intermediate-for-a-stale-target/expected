oracle GNU Make 4.4.1
outcome success
file Makefile kept 7
| # The same shape with hello.tsk behind hello.x: what reads the excused file
| # has to run, so the file is asked for after all.
| .SECONDARY:
| all: hello.tsk
| hello.x: ; echo body > $@
| %.z: %.x ; cat $< > $@
| %.tsk: %.z ; cat $< > $@
file hello.tsk touched 1
| body
file hello.x kept 1
| body
file hello.z touched 1
| body
