oracle GNU Make 4.4.1
outcome success
file Makefile kept 13
| # The same shape under -n. GNU Make turns the switch off across the makefile
| # update, so helper, the child and gen.mk are all made for real and only the
| # goal is described.
| all: ; @printf '%s\n' '$(GENERATED)' > out
| 
| include gen.mk
| 
| gen.mk: helper
| 	@printf 'GENERATED := from-generated\n' > $@
| 
| helper:
| 	@printf 'h\n' > helper
| 	@$(MAKE) -C sub marker
file gen.mk touched 1
| GENERATED := from-generated
file helper touched 1
| h
file sub/Makefile kept 1
| marker: ; @printf %s child-ran > marker.out
file sub/marker.out touched 2
| child-ran
