oracle GNU Make 4.4.1
outcome success
file Makefile kept 5
| # A rejected target does not end the offer: Make considers the next target of
| # the same rule, exactly as the leading-dot rule does.
| .SUFFIXES:
| .SUFFIXES: foo bar
| foo zed: ; @printf '%s\n' 'ran $@' >> out
file out touched 1
| ran zed
