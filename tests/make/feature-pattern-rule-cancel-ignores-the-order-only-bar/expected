oracle GNU Make 4.4.1
outcome failure
file Makefile kept 6
| # The comparison reads prerequisite names as one chain, so which side of the
| # bar a name was written on does not tell two rules apart.
| %.gen: %.src | %.alt ; @printf '%s\n' built > $@
| %.gen: %.src %.alt
| 
| all: foo.gen
file foo.alt kept 1
| alternative
file foo.src kept 1
| source
