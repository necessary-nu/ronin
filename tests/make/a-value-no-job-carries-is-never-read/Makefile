# A command-line assignment whose value holds an unterminated reference.
# GNU Make expands an exported recursive value only where it materialises an
# environment for something it is starting, so a goal with no recipe behind it
# starts nothing, reads nothing, and refuses nothing.
all:
