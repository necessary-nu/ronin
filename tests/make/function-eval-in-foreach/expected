oracle GNU Make 4.4.1
outcome success
file Makefile kept 4
| VARS = A B
| VARSET = $(1) = $(2)
| $(foreach v,$(VARS),$(eval $(call VARSET,$(v),$(v))))
| all: ; @printf '%s\n' 'A = $(A) B = $(B)' > out
file out touched 1
| A = A B = B
