oracle GNU Make 4.4.1
outcome failure
file Makefile kept 7
| # A question runs the marked lines it meets before the first unmarked one and
| # reads their status, and answers over the first line that is not marked. The
| # marked line is written first here for that reason: written second it would
| # never be reached, which is the boundary and not the behaviour.
| goal:
| 	+@echo marked > marked.out
| 	@echo one > one.out
file marked.out touched 1
| marked
