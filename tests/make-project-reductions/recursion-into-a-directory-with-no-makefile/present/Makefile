# A child that does exist, reached because the invocation naming it composes.
# The census descends with the composition, so what this file holds has to
# survive the sibling that could not be read.
all:
	@if test -n "$(WHEN)"; then cd .. && $(MAKE) all; fi
