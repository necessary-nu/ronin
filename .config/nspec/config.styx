// The source impl tracks the original C symbols. The target impl
// requires corresponding Rust implementation and verification sites.
specs ({
    name samurai,
    include (docs/spec/**/*.md),
    impls (
        {name source-impl, languages (@c), include (**/*.c **/*.h), mode @impl-only}
        {name target-impl, languages (@rust), include (src/**/*.rs crates/**/*.rs), exclude (target/**), mode @both}
    )
})
